# -*- snakemake -*-
config_default = {
    'bio.ngs.popgen.ngstools' : {
        'ngsfst' : {
            'cmd' : os.path.join(config['bio.ngs.popgen.ngstools']['home'], "ngsFST"),
        },
    },
}

update_config(config_default, config)
config = config_default

